services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - ${HOME}/Documents/kozakura/apps_data/flask_template_app/.env
    environment:
      PORT: "5000"
    volumes:
      - ${HOME}/Documents/kozakura/apps_data/flask_template_app:/data
    restart: unless-stopped
